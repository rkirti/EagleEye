#include "circuit.h"
#include "atpg.h"


bool Do_ATPG()
{
    /*
	map<string,Wire *>::iterator iter1=circuit.Netlist.begin();

	iter1->second->value = ZERO;
	iter1++;
	iter1->second->value = ONE;
	map<string,Gate *>::iterator iter2;

	for (iter2=circuit.Gates.begin(); iter2 != circuit.Gates.end(); iter2++)
	{
		cout << iter2->first << endl;
		iter2->second->output->value = iter2->second->Evaluate();
	}*/
    
	return true;
}
